name: babycare
description: Baby monitoring and care app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  image_picker: ^1.0.4
  shared_preferences: ^2.2.2
  fl_chart: ^0.64.0
  flutter_local_notifications: ^16.3.0
  assets_audio_player: ^3.0.3+6
  audioplayers: ^5.2.1
  timezone: ^0.9.4
  http: ^1.1.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/notification_sound.wav
